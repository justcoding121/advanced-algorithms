using System;

namespace Algorithm.Sandbox.String
{
    public partial class StringAlgorithms
    {
        /// <summary>
        /// determine if the string has all unique characters
        /// </summary>
        /// <param name="input"></param>
        /// <returns></returns>
        public bool IsAllUniqueChars(string input)
        {
            var chars = input.ToCharArray();
            //TODO
            //Can we use ASCII values or bit manipulation?
            throw new NotImplementedException();
        }
    }
}
